# Toolchain
.venv/
firmware/.esphome/

# Credentials — generated by firmware/make-secrets.sh
firmware/secrets.yaml

# Build output, produced by installer/build.sh and published by CI
installer/manifest.json
installer/*.bin

# Mechanical design files stay out of the public repository: they embed the
# Nexus logo lockup as an SVG. Keep them locally or in a private repo.
*.3mf
*.stl

# Internal project notes
Nexus_Lampe_Projekt_Briefing.md

.DS_Store
